version: '1.0'
steps:

  building_image:
    type: build
    description: BUT's SDK image
    dockerfile: sdk/Dockerfile
    image_name: but-sdk
    tag: latest

  gcc_Debug_none:
    image: but-sdk:latest
    working_directory: ${{main_clone}}
    commands:
      - ./ci/gcc_Debug_none
