version: '1.0'
steps:

  build_image:
    type: build
    description: BUT's SDK image
    dockerfile: Dockerfile
    working_directory: sdk
    image_name: but-sdk
    tag: ${{CF_BRANCH}}

  gcc_Debug_none:
    image: ${{build_image}}
    commands:
      - ./ci/gcc_Debug_none
