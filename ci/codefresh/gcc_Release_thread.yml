version: '1.0'
steps:

  build_image:
    type: build
    description: BUT's SDK image
    dockerfile: Dockerfile
    working_directory: sdk
    image_name: but-sdk
    tag: ${{CF_BRANCH}}

  gcc_Release_thread:
    image: ${{build_image}}
    working_directory: ${{main_clone}}
    commands:
      - ./ci/gcc_Release_thread --clean-build
