version: '1.0'
steps:

  build_image:
    type: build
    description: BUT's SDK image
    dockerfile: Dockerfile
    working_directory: sdk
    image_name: but-sdk
    tag: ${{CF_BRANCH}}

  clang_Relwithdebinfo_none:
    image: ${{build_image}}
    working_directory: ${{main_clone}}
    commands:
      - ./ci/clang_Relwithdebinfo_none --clean-build
